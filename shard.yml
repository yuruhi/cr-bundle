name: cr-bundle
version: 0.1.0
description: |
  A cli tool for bundling Crystal language's source codes into a single file.

authors:
  - yuruhi

targets:
  cr-bundle:
    main: src/cli.cr

dependencies:
  cr-bundle:
    github: yuruhi/cr-bundle

crystal: 0.36.1

license: MIT
