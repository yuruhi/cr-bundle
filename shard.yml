name: cr-bundle
version: 0.1.0
description: |
  A cli tool for bundling Crystal language's source codes into a single file.

authors:
  - yuruhi

targets:
  cr-bundle:
    main: src/cli.cr

crystal: 0.36.1

license: MIT
